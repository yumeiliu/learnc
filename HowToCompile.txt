对于C语言：
使用GCC来编译和链接
对于C++语言：
使用G++来编译和链接

指令:
g++ -c filename.cpp(当前路径.cpp文件)    // 只编译，不链接，生成.o 文件
g++  filename.cpp(当前路径.cpp文件)        // 既编译，同时链接，生成可执行文件(默认是a.out), gcc编译程序，编译完成后，在你的文件中出现一个./a.out运行文件。
指令：./a.out
    运行编译好的文件
